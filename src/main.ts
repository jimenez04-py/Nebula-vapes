import { bootstrapApplication } from '@angular/platform-browser';
import { CatalogComponent } from './app/components/catalog/catalog.component';

bootstrapApplication(CatalogComponent).catch(console.error);
